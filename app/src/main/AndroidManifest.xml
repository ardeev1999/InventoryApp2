<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InventoryApp">
        
        <!-- Главная активность -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.InventoryApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Активность со списком предметов -->
        <activity
            android:name=".ItemsListActivity"
            android:exported="false"
            android:label="Список предметов"
            android:theme="@style/Theme.InventoryApp" />
        
        <!-- Активность сканирования (ДОБАВЬТЕ ЭТОТ БЛОК ОТДЕЛЬНО) -->
        <activity
            android:name=".ScanActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- НОВАЯ АКТИВНОСТЬ -->
        <activity
            android:name=".ScannedItemsActivity"
            android:label="Найденные предметы"
            android:parentActivityName=".MainActivity" />   
            
    </application>

</manifest>